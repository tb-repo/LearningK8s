apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data-static
  namespace: tb-app-storage
  labels:
    app: postgres
    storage-demo: static
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi

