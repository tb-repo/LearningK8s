apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: tb-app-storage
spec:
  type: ClusterIP
  selector:
    app: postgres
    tier: data
    storage-demo: static
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432

