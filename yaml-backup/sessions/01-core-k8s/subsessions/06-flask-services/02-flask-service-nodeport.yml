apiVersion: v1
kind: Service
metadata:
  name: flask-web-nodeport
  namespace: tb-app-core
spec:
  type: NodePort
  selector:
    app: flask-web
    tier: application
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080

