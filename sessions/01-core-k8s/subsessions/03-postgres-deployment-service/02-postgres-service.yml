apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: tb-app-core
spec:
  type: ClusterIP
  selector:
    app: postgres
    tier: data
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432

