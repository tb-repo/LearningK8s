apiVersion: v1
kind: Service
metadata:
  name: flask-web-clusterip
  namespace: tb-app-core
spec:
  type: ClusterIP
  selector:
    app: flask-web
    tier: application
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000

