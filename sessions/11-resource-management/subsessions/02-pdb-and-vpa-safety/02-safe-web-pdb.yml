apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: safe-web
  namespace: app-scaling-prod
  labels:
    app: safe-web
    scaling-demo: pdb-vpa
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: safe-web
      scaling-demo: pdb-vpa
